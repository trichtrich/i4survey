<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="i4survey.list">
      <field name="name">i4survey list</field>
      <field name="model">i4survey.i4survey</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="i4survey.action_window">
      <field name="name">i4survey window</field>
      <field name="res_model">i4survey.i4survey</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="i4survey.action_server">
      <field name="name">i4survey server</field>
      <field name="model_id" ref="model_i4survey_i4survey"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="i4survey" id="i4survey.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="i4survey.menu_1" parent="i4survey.menu_root"/>
    <menuitem name="Menu 2" id="i4survey.menu_2" parent="i4survey.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="i4survey.menu_1_list" parent="i4survey.menu_1"
              action="i4survey.action_window"/>
    <menuitem name="Server to list" id="i4survey" parent="i4survey.menu_2"
              action="i4survey.action_server"/>
    -->
  </data>
</odoo>